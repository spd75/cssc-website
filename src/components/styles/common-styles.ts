export const FlexCC = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
};

export const HeightWidth = (height: string, width: string) => ({
    height: height,
    width: width
});
